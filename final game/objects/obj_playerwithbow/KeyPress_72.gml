
  if (sprite_index == spr_playerwithbow) {
        // If the player is facing right
        arrow = instance_create_layer(x, y, "Bow", obj_arrow);
        arrow.direction = 0; // Right
        arrow.speed = 15; // Adjust speed as needed
    }







